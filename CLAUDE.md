# Git運用ルール(このフォルダ専用)

このフォルダはGitHubリポジトリ `Cipher-implosion/-` と連携しています(ローカルの `資格対策` フォルダ = リポジトリのルート)。

## セッション開始時
- 作業を始める前に `git pull` を実行し、最新の状態にしてから作業に着手すること。

## 作業終了時
- 作業の区切り(ユーザーから「終わり」「ここまで」等の指示があった時、または一連の変更が完了した時)には、以下を行うこと。
  1. `git status` / `git diff` で変更内容を確認する
  2. 変更内容に応じたコミットメッセージ案を提示する
  3. ユーザーに実行の確認を取ってから `git add` → `git commit` → `git push` を行う
- ユーザーの確認なしにpushしないこと。
